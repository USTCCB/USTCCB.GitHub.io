{
  "name": "ustccb-cloudflare-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.83.0"
  }
}
